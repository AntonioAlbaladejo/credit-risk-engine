name: CD - Build and Deploy

on:
  push:
    branches: [ main ]
  workflow_run:
    workflows: [ "CI - Lint and Test" ]
    types: [ completed ]
    branches: [ main ]

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event.workflow_run.conclusion == 'success'
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Extract version from pyproject.toml
        id: version
        run: |
          VERSION=$(grep '^version' pyproject.toml | sed 's/version = "\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
      
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: |
            credit-risk-engine:latest
            credit-risk-engine:${{ steps.version.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=docker,dest=/tmp/image.tar
      
      - name: Upload Docker image
        uses: actions/upload-artifact@v3
        with:
          name: docker-image
          path: /tmp/image.tar
          retention-days: 1

  test-image:
    name: Test Docker Image
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      
      - name: Download Docker image
        uses: actions/download-artifact@v3
        with:
          name: docker-image
          path: /tmp
      
      - name: Load image
        run: |
          docker load --input /tmp/image.tar
      
      - name: Test image
        run: |
          docker run --rm credit-risk-engine:latest python -c "import src; print('Import successful')"

  # Uncomment and configure when ready to push to a registry
  # push:
  #   name: Push to Registry
  #   runs-on: ubuntu-latest
  #   needs: test-image
  #   steps:
  #     - uses: actions/checkout@v4
  #     
  #     - name: Download Docker image
  #       uses: actions/download-artifact@v3
  #       with:
  #         name: docker-image
  #         path: /tmp
  #     
  #     - name: Load image
  #       run: |
  #         docker load --input /tmp/image.tar
  #     
  #     - name: Push to Docker Hub
  #       run: |
  #         # Configure your registry credentials and push commands here
  #         echo "Push to your registry"
