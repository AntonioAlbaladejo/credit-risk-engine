# Git
.git
.gitignore

# Python
__pycache__
*.pyc
*.pyo
*.pyd
.pytest_cache
.mypy_cache
*.egg-info
dist/
build/

# Development
.vscode
.claude
.mcp.json
.benchmarks/
.env
.env.local
venv/
.venv/
.conda/
CLAUDE.md

# Notebooks (not needed in production)
notebooks/

# Data and results
data/
results/

# Source HTML for the regulatory corpus: reingestion material, and untracked,
# so leaving it in would make the image size depend on the developer's disk.
# The chunked JSONL stays in: retrieval will read it from the service.
corpus/raw/

# Tests, docs and CI config (not needed at runtime)
tests/
docs/
assets/
scripts/
.github/
.coverage
coverage.xml
.ruff_cache

# MLflow local tracking store and artifacts
mlruns/
mlartifacts/
mlflow.db
mlflow.db-shm
mlflow.db-wal

# Others
.DS_Store
README.md
LICENSE
docker-compose.yml